<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nbastatsquiz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_stats_quiz"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_stats_quiz"
        android:supportsRtl="true"
        android:theme="@style/Theme.NBAQUIZ"
        tools:targetApi="lollipop">
        <activity android:name="com.nbastatsquiz.NavigationDrawerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.nbastatsquiz.GameActivity" />
    </application>

</manifest>